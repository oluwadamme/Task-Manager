
module.exports = {
    setApiKey(){
        // we are not returning any value because the real module does not return any value when it is called
    },
    send(){
         // we are not returning any value because the real module does not return any value when it is called
    }
}